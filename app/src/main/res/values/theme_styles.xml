<resources>

    <!-- Base application theme. -->
    <style name="DarkTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:fontFamily">monospace</item>
        <item name="android:textColorHint">@color/primary_hint_text</item>
        <item name="android:textColor">@color/primary_light_accent_text</item>


        <!--<item name="android:actionMenuTextColor">@color/primary_dark_text</item>-->
        <!--<item name="android:me">@color/primary_dark_text</item>-->
        <!--<item name="android:itemBackground">@color/primary_dark_text</item>-->


        <item name="actionBarItemBackground">@color/primary_dark_accent_background</item>
        <!-- To change the background of options menu-->
        <item name="android:itemBackground">@color/primary_dark_accent_background</item>
    </style>

    <style name="DarkTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
